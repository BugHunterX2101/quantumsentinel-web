<div align="center">

<img src="frontend/favicon.png" alt="QuantumSentinel Logo" width="72" height="72">

# QuantumSentinel

### The World's First Open-Source Post-Quantum Secure Trading Terminal

[![Python](https://img.shields.io/badge/Python-3.11%2B-blue?logo=python&logoColor=white)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.110%2B-009688?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)
[![FIPS 203](https://img.shields.io/badge/FIPS_203-ML--KEM--768-cyan)](https://csrc.nist.gov/pubs/fips/203/final)
[![FIPS 204](https://img.shields.io/badge/FIPS_204-ML--DSA--65-purple)](https://csrc.nist.gov/pubs/fips/204/final)
[![License](https://img.shields.io/badge/License-Apache_2.0-green)](LICENSE)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?logo=docker&logoColor=white)](docker-compose.yml)
[![Paper Trading](https://img.shields.io/badge/Trading-Paper_Only-orange)](SECURITY.md)

*Quantum-safe cryptography · Real market signals · Signed audit trail · Zero real money*

</div>

---

> **Safety boundary:** QuantumSentinel is paper-trading software. It is not a live brokerage, custodian, financial adviser, or production-certified cryptographic service. Never connect production financial credentials without completing the hardening steps in [SECURITY.md](SECURITY.md).

---

## 🔍 What Is QuantumSentinel?

QuantumSentinel is a **production-architecture reference implementation** of a post-quantum secure trading terminal. It demonstrates how NIST's finalized post-quantum cryptography standards (FIPS 203 / FIPS 204) can be applied end-to-end in a real web application — from the browser's TLS handshake replacement all the way down to individual signed order records in the database.

Every session begins with a **hybrid X25519 + ML-KEM-768 key exchange** (combining classical and post-quantum cryptography), every order is signed with **ML-DSA-65**, and every audit log entry is tamper-evidenced with a lattice-based signature — all verifiable in real time from the browser dashboard.

Signals are generated by a **Simulated Bifurcation Algorithm (SBA)** — a quantum-inspired combinatorial optimizer that runs on standard CPUs using real Yahoo Finance market data, correlating assets via a rolling Pearson coupling matrix.

---

## ✨ Feature Matrix

| Capability | Implementation | Standards |
|---|---|---|
| **PQC Handshake** | Hybrid X25519 + ML-KEM-768 via HKDF-SHA256 | FIPS 203, RFC 7748 |
| **Order Signing** | ML-DSA-65 signs every order payload | FIPS 204 |
| **Audit Trail** | Server-signed `AuditLog` table with in-browser verification | FIPS 204 |
| **Signal Engine** | Simulated Bifurcation Algorithm (SBA) + RSI, MACD, momentum, Bollinger width | Custom |
| **Real-time Feed** | Authenticated WebSocket stream with exponential backoff reconnect | RFC 6455 |
| **Paper Trading** | Alpaca paper API integration with local market-price fallback simulator | Alpaca v2 |
| **Order Controls** | Market/Limit/Stop/Stop-Limit, Day/GTC/IOC, duplicate guard, oversell prevention, dynamic 5% cap | — |
| **Strategy Builder** | Visual MA crossover editor + historical backtests with Sharpe, drawdown, win rate | — |
| **Portfolio Risk** | Positions, unrealized P&L, equity curve (mark-to-market), Sharpe, VaR 95/99, CSV export | — |
| **Key Management** | Per-user ML-KEM-768 + ML-DSA-65 key rotation, Quantum Safety Score ring | FIPS 203/204 |
| **Enterprise SDK** | Scoped `X-QS-API-KEY` credentials (read / trade / admin) with SHA-256 stored hash | — |
| **Signed Webhooks** | HTTPS-only, HMAC-SHA256 signed events, Fernet-encrypted secrets at rest | RFC 2104 |
| **Prometheus Metrics** | HTTP request counts, latency histograms, behind auth guard | OpenMetrics |
| **Beginner Mode** | 5-step onboarding, contextual plain-language tips, community hub | — |
| **Mobile Responsive** | Horizontally scrollable 7-tab navigation, responsive grid breakpoints | CSS3 |

---

## 🏗️ System Architecture

### High-Level Component Map

```mermaid
flowchart TB
    subgraph Client["🖥️ Browser Client"]
        FE["Vanilla JS + Web Crypto API<br/>SPA — no build step required"]
    end

    subgraph Gateway["⚡ FastAPI Gateway (main.py)"]
        MW["Security Middleware<br/>CORS · Rate Limiter · Security Headers · CSP"]
        AUTH_MW["JWT Bearer Auth<br/>Depends(get_current_user)"]
        WS["WebSocket Signal Stream<br/>/api/signals/stream"]
        SPA["SPA Catch-all<br/>/{path:path} → index.html"]
    end

    subgraph Services["🔧 Service Layer"]
        AUTH["auth_service.py<br/>JWT · PBKDF2 · PQC Handshake<br/>Nonce TTL · Session store"]
        SIG["signal_engine.py<br/>SBA · RSI · MACD · Bollinger<br/>Correlation matrix · Cache lock"]
        TRADE["trading_service.py<br/>Order lifecycle · Alpaca SDK<br/>Market simulator · Fill logic"]
        PORT["portfolio_service.py<br/>Positions · Equity curve<br/>Sharpe · VaR · Drawdown"]
        SEC["security_service.py<br/>Server ML-DSA identity<br/>Audit log · Key health<br/>90-day rotation policy"]
        BACK["backtest_service.py<br/>MA crossover · Win rate<br/>Round-trip tracking"]
        INTG["integration_service.py<br/>API keys · Webhooks<br/>HMAC signing · SSRF guard"]
    end

    subgraph Crypto["🔐 PQC Crypto Layer (pqc.py)"]
        KEM["ML-KEM-768<br/>FIPS 203<br/>1184B pk · 1088B ct · 32B ss"]
        DSA["ML-DSA-65<br/>FIPS 204<br/>1952B pk · 3309B sig"]
        X25519["X25519<br/>RFC 7748<br/>Classical hybrid leg"]
        HKDF["HKDF-SHA256<br/>Session key derivation<br/>client‖server nonce salt"]
    end

    subgraph Persistence["💾 Persistence"]
        DB[("SQLite / PostgreSQL<br/>SQLAlchemy 2.0")]
        REDIS[("Redis<br/>Rate limiting · Sessions<br/>(in-memory fallback in dev)")]
    end

    subgraph External["🌐 External APIs"]
        YF["Yahoo Finance<br/>3-month OHLCV data"]
        ALPACA["Alpaca Paper API<br/>paper-api.alpaca.markets"]
        GH["GitHub API<br/>Community hub"]
    end

    Client <-->|"HTTPS / WSS<br/>Bearer JWT"| Gateway
    Gateway --> AUTH_MW --> Services
    Gateway --> WS --> SIG
    Gateway --> SPA

    AUTH --> Crypto
    SEC --> Crypto
    TRADE --> Crypto

    AUTH --> DB
    TRADE --> DB
    PORT --> DB
    SEC --> DB
    INTG --> DB

    AUTH --> REDIS
    MW --> REDIS

    SIG --> YF
    TRADE --> ALPACA
    Client -->|"Public read-only"| GH

    KEM --- HKDF
    X25519 --- HKDF
```

---

### PQC Hybrid Handshake Protocol

```mermaid
sequenceDiagram
    participant BR as 🖥️ Browser<br/>(Web Crypto API)
    participant GW as ⚡ FastAPI Gateway
    participant PQC as 🔐 PQC Service<br/>(pqc.py)
    participant DB as 💾 Database

    Note over BR,DB: Phase 1 — Authentication
    BR->>GW: POST /api/auth/login<br/>{email, password}
    GW->>DB: PBKDF2-SHA256 verify (200,000 rounds)
    DB-->>GW: User record
    GW-->>BR: RS256 JWT (15 min TTL) + expires_in

    Note over BR,DB: Phase 2 — Hybrid PQC Handshake
    BR->>BR: Generate X25519 ephemeral keypair<br/>(Web Crypto subtle.generateKey)
    BR->>BR: Generate 32-byte client nonce<br/>(crypto.getRandomValues)
    BR->>GW: POST /api/auth/pqc-handshake<br/>{x25519_pub_b64, client_nonce_b64}

    GW->>PQC: x25519_keygen() — server ephemeral
    GW->>PQC: kem_keygen() — ML-KEM-768 server key
    PQC-->>GW: Server KEM keypair (1184B pk, 2400B sk)

    GW->>PQC: kem_encapsulate(server_kem_pk)<br/>→ ciphertext (1088B) + KEM_secret (32B)
    GW->>PQC: x25519_shared_secret(server_sk, client_x25519_pub)
    GW->>PQC: derive_session_key(<br/>  x25519_secret ‖ kem_secret,<br/>  salt=client_nonce‖server_nonce<br/>)
    Note right of PQC: session_key = HKDF-SHA256(32B)

    GW->>PQC: dsa_sign(server_dsa_sk, ServerHello_payload)<br/>ML-DSA-65 signature (3309B)
    GW->>DB: Persist session + KEM keypair
    GW-->>BR: ServerHello {<br/>  kem_ciphertext_b64,<br/>  server_x25519_pub_b64,<br/>  server_nonce_b64,<br/>  ml_dsa_signature_b64,<br/>  session_id,<br/>  algorithm_sizes<br/>}

    BR->>BR: Verify ML-DSA-65 signature (displayed in trace)
    Note over BR: ✅ Quantum-safe session established
```

---

### Order Lifecycle & Audit Flow

```mermaid
sequenceDiagram
    participant UI as 🖥️ UI / SDK Client
    participant GW as ⚡ Gateway
    participant TS as 📈 Trading Service
    participant PQC as 🔐 PQC Layer
    participant ALP as 🏦 Alpaca Paper API
    participant DB as 💾 Database
    participant WH as 🔔 Webhook Consumer

    UI->>GW: POST /api/trading/orders<br/>{asset, side, qty, type, time_in_force, ...}
    GW->>GW: JWT auth + rate limit check

    Note over GW,DB: Pre-trade validations
    GW->>DB: Fetch open positions
    GW->>GW: Oversell check (qty ≤ held)
    GW->>GW: Position cap: 5% × account_equity
    GW->>DB: Duplicate trade guard (15s window)

    alt Alpaca configured
        TS->>ALP: Submit paper order
        ALP-->>TS: Order status (ACCEPTED/FILLED/REJECTED)
    else Local simulator
        TS->>TS: simulate_fill(asset, side, qty)<br/>get_last_price() via yfinance
        TS->>TS: IOC → EXPIRED if partial fill
    end

    TS->>PQC: ML-DSA-65 sign order payload
    PQC-->>TS: Signature (3309B)

    TS->>DB: INSERT Trade {status, filled_price, pqc_signature}
    TS->>DB: recompute_positions(user_id)
    TS->>PQC: ML-DSA-65 sign AuditLog payload
    TS->>DB: INSERT AuditLog {action, pqc_signature}

    TS->>WH: emit_webhooks(order.filled)<br/>HMAC-SHA256 signed envelope
    WH-->>TS: 200 OK (best-effort, never blocks)

    GW-->>UI: OrderResponse {status, filled_price, ...}
```

---

### Signal Engine Pipeline

```mermaid
flowchart LR
    subgraph Input["📥 Data Ingestion"]
        YF["yfinance.download()<br/>3-month OHLCV<br/>12 assets"]
    end

    subgraph Features["🧮 Feature Extraction (per asset)"]
        RSI["RSI-14<br/>Momentum oscillator"]
        MACD["MACD Histogram<br/>12/26/9 EMA diff"]
        MOM["20-day Momentum<br/>% price change"]
        BB["Bollinger Width<br/>Volatility proxy"]
    end

    subgraph Coupling["🔗 Cross-Asset Coupling Matrix"]
        CORR["Pearson Correlation<br/>20-bar daily returns<br/>N×N coupling matrix J"]
        ZSCORE["Cross-sectional z-score<br/>Idiosyncratic momentum h_i<br/>h = clip((mom - μ)/σ, -3, 3) × 0.35"]
    end

    subgraph SBA["⚛️ Simulated Bifurcation Algorithm"]
        INIT["Initialize spins s_i ∈ {-1, +1}"]
        ITER["1000 iterations<br/>Pressure ramp Δ(t)"]
        UPDATE["s_i ← sign(J·s + h + Δ·s_i)"]
        CONV["Converged spin state"]
    end

    subgraph Scoring["🎯 Signal Scoring"]
        SCORE["score_signal(spin_i, rsi_i)<br/>spin > 0.2 + rsi < 70 → BUY<br/>spin < -0.2 + rsi > 30 → SELL<br/>else HOLD"]
        CONF["Confidence = |spin| × 0.6 + rsi_adj × 0.4"]
    end

    subgraph Cache["💾 Thread-safe Cache"]
        LOCK["threading.Lock()<br/>20-min TTL<br/>Stale-on-error fallback"]
    end

    YF --> Features
    Features --> Coupling
    Coupling --> SBA
    SBA --> Scoring
    Scoring --> Cache
    Cache -->|"GET /api/signals/latest<br/>WS /api/signals/stream"| API["API Layer"]
```

---

### Database Schema

```mermaid
erDiagram
    users {
        string id PK
        string email UK
        string password_hash
        boolean is_admin
        boolean is_active
        datetime created_at
    }

    trades {
        string id PK
        string user_id FK
        string asset
        string side
        float quantity
        string order_type
        string time_in_force
        float limit_price
        float stop_price
        float filled_price
        string status
        string pqc_signature
        datetime submitted_at
        datetime filled_at
    }

    positions {
        string id PK
        string user_id FK
        string asset
        float quantity
        float avg_entry_price
        float realized_pnl
        datetime updated_at
    }

    key_pairs {
        string id PK
        string user_id FK
        string algorithm
        text public_key_b64
        text private_key_enc
        boolean is_active
        int rotation_count
        datetime created_at
    }

    audit_logs {
        string id PK
        string user_id FK
        string action
        string resource_type
        string resource_id
        json metadata_json
        string pqc_signature
        datetime created_at
    }

    strategies {
        string id PK
        string user_id FK
        string name
        json assets
        json config
        datetime created_at
    }

    api_keys {
        string id PK
        string user_id FK
        string name
        string prefix
        string key_hash
        json scopes
        boolean is_revoked
        datetime expires_at
        datetime last_used_at
    }

    webhooks {
        string id PK
        string user_id FK
        string url
        json event_types
        string secret_hash
        boolean is_active
        datetime last_delivery_at
    }

    users ||--o{ trades : "places"
    users ||--o{ positions : "holds"
    users ||--o{ key_pairs : "owns"
    users ||--o{ audit_logs : "generates"
    users ||--o{ strategies : "creates"
    users ||--o{ api_keys : "manages"
    users ||--o{ webhooks : "configures"
```

---

### Security Middleware Pipeline

```mermaid
flowchart TD
    REQ["Incoming HTTP Request"] --> CORS["CORS check\nAllowed origins from config"]
    CORS --> HOST["Trusted Host check\nAllowed hosts from config"]
    HOST --> RATE["Rate Limiter\nRedis INCR / in-memory fallback\nPer-IP sliding window"]
    RATE -->|"429 Too Many Requests"| REJECT["❌ Reject"]
    RATE -->|"OK"| ROUTE["Route Handler"]
    ROUTE --> AUTH["JWT Dependency\nget_current_user()\nRS256 verify"]
    AUTH -->|"401 Unauthorized"| REJECT
    AUTH -->|"OK"| SVC["Service Layer"]
    SVC --> RESP["Response"]
    RESP --> HDR["Security Headers\nX-Frame-Options: DENY\nX-Content-Type-Options: nosniff\nReferrer-Policy: no-referrer\nPermissions-Policy: camera=(), mic=()\nCSP: default-src 'self'; wss:; frame-ancestors 'none'\nX-RateLimit-Remaining: N"]
    HDR --> METRICS["Prometheus Counter/Histogram\nHTTP_REQUESTS, HTTP_LATENCY\n/metrics (auth-gated)"]
    METRICS --> CLIENT["↩ Client"]
```

---

## 📁 Repository Structure

```
quantumsentinel-web/
│
├── backend/                          # FastAPI application
│   ├── main.py                       # 750-line central router, middleware, WS stream, SPA fallback
│   ├── models.py                     # SQLAlchemy 2.0 declarative schema (8 tables)
│   ├── schemas.py                    # Pydantic v2 request / response validation
│   ├── database.py                   # Engine, session factory, init_db(), compat migration
│   ├── config.py                     # ENV-driven config with production safety constraints
│   │
│   ├── crypto/
│   │   ├── __init__.py
│   │   └── pqc.py                    # ML-KEM-768, ML-DSA-65, X25519, HKDF, algorithm registry
│   │
│   └── services/
│       ├── auth_service.py           # JWT, PBKDF2, PQC handshake, nonce TTL store
│       ├── signal_engine.py          # SBA, feature extraction, correlation matrix, thread-safe cache
│       ├── trading_service.py        # Order lifecycle, Alpaca SDK, local simulator, fill logic
│       ├── portfolio_service.py      # Positions, mark-to-market equity curve, Sharpe, VaR
│       ├── security_service.py       # Server ML-DSA identity, audit log, key health, 90-day rotation
│       ├── backtest_service.py       # MA crossover backtest, round-trip win rate, equity curve
│       └── integration_service.py   # Scoped API keys, SSRF-guarded signed webhooks, Fernet secrets
│
├── frontend/                         # Vanilla JS SPA (no build step)
│   ├── index.html                    # App shell, SEO meta, 7-tab navigation, all forms
│   ├── app.js                        # ~700-line SPA: auth, handshake, trading, portfolio, WS
│   ├── styles.css                    # Design system: glassmorphism, animations, mobile-responsive
│   ├── favicon.ico                   # Quantum diamond icon (16×16, 32×32)
│   └── robots.txt                    # Blocks /api/ and /health/ from indexing
│
├── deploy/                           # Production deployment configs
│   ├── nginx.conf                    # TLS 1.3 reverse proxy with HSTS
│   └── tls/                          # Certificate mount point
│
├── .env.example                      # All environment variables documented
├── Dockerfile                        # Multi-stage Python image
├── docker-compose.yml                # Dev/demo — SQLite volume, single container
├── docker-compose.production.yml     # Prod — PostgreSQL, Redis, Nginx, Gunicorn, backup
├── requirements.txt                  # Pinned dependencies (no upper bounds in dev)
├── SECURITY.md                       # Threat model, hardening checklist, responsible disclosure
├── LICENSE                           # Apache-2.0
└── README.md                         # This file
```

---

## ⚡ Quick Start

### Prerequisites
- Python 3.11+ (3.12 recommended)
- Git

### 1 — Clone & Install

```bash
git clone https://github.com/BugHunterX2101/quantumsentinel-web.git
cd quantumsentinel-web

python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS / Linux:
source .venv/bin/activate

pip install -r requirements.txt
```

### 2 — Configure (optional)

```bash
# Windows
copy .env.example .env
# macOS / Linux
cp .env.example .env
```

Set `ALPACA_API_KEY` + `ALPACA_SECRET_KEY` for live paper execution via Alpaca. Without them, orders settle against the latest Yahoo Finance price in the built-in simulator.

### 3 — Run

```bash
uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
```

Open **http://127.0.0.1:8000** → register → complete the onboarding tour → place a paper trade.

Interactive API docs: **http://127.0.0.1:8000/docs**

---

## 🐳 Docker

### Development (SQLite)

```bash
docker compose up --build
# Open: http://localhost:8000
```

### Production (PostgreSQL + Redis + Nginx TLS)

```bash
# 1. Generate RSA keypair for JWT RS256
python -c "
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.primitives import serialization
k = rsa.generate_private_key(65537, 2048)
print('PRIVATE:', k.private_bytes(serialization.Encoding.PEM, serialization.PrivateFormat.TraditionalOpenSSL, serialization.NoEncryption()).decode().replace('\n','\\n'))
print('PUBLIC:', k.public_key().public_bytes(serialization.Encoding.PEM, serialization.PublicFormat.SubjectPublicKeyInfo).decode().replace('\n','\\n'))
"

# 2. Copy and fill production env
copy .env.production.example .env.production   # Windows
# cp .env.production.example .env.production   # macOS/Linux

# 3. Put real TLS certs at deploy/tls/fullchain.pem and deploy/tls/privkey.pem

# 4. Deploy
docker compose -f docker-compose.production.yml --env-file .env.production up -d --build
```

> [!IMPORTANT]
> Production mode deliberately refuses the bundled pure-Python reference PQC backend.
> Set `PQC_PROVIDER` and `PQC_PROVIDER_URL` only after integrating a reviewed liboqs/HSM adapter.

---

## 🔌 API Reference

| Area | Method | Endpoint | Auth |
|---|---|---|---|
| **Auth** | `POST` | `/api/auth/register` | — |
| | `POST` | `/api/auth/login` | — |
| | `POST` | `/api/auth/pqc-handshake` | JWT |
| **Signals** | `GET` | `/api/signals/latest` | JWT |
| | `POST` | `/api/signals/refresh` | JWT |
| | `WS` | `/api/signals/stream` | JWT (subprotocol) |
| **Trading** | `POST` | `/api/trading/orders` | JWT |
| | `GET` | `/api/trading/orders` | JWT |
| | `DELETE` | `/api/trading/orders/{id}` | JWT |
| **Strategies** | `GET` | `/api/strategies/templates` | JWT |
| | `GET/POST` | `/api/strategies` | JWT |
| | `GET/POST` | `/api/backtests` | JWT |
| **Portfolio** | `GET` | `/api/portfolio/positions` | JWT |
| | `GET` | `/api/portfolio/risk-metrics` | JWT |
| | `GET` | `/api/portfolio/export` | JWT |
| **Security** | `GET` | `/api/security/health` | JWT |
| | `GET` | `/api/security/audit-log` | JWT |
| | `POST` | `/api/security/rotate-keys` | JWT |
| | `GET` | `/api/security/compliance-report` | JWT |
| **SDK** | `GET` | `/api/sdk/portfolio` | `X-QS-API-KEY` (read) |
| | `POST` | `/api/sdk/orders` | `X-QS-API-KEY` (trade) |
| **Integrations** | `GET/POST` | `/api/integrations/api-keys` | JWT |
| | `GET/POST` | `/api/integrations/webhooks` | JWT |
| **Meta** | `GET` | `/api/meta` | — |
| | `GET` | `/api/algorithms` | — |
| **Observability** | `GET` | `/metrics` | JWT |
| | `GET` | `/health/live` | — |
| | `GET` | `/health/ready` | — |

---

## ⚙️ Configuration

All variables are documented in [`.env.example`](.env.example):

| Variable | Required | Description |
|---|---|---|
| `ENVIRONMENT` | No | `development` (default) or `production` |
| `DATABASE_URL` | No | SQLite by default; set Postgres DSN for production |
| `JWT_PRIVATE_KEY` | Prod | RS256 private key (PEM, `\n`-escaped) |
| `JWT_PUBLIC_KEY` | Prod | RS256 public key (PEM, `\n`-escaped) |
| `WEBHOOK_ENCRYPTION_KEY` | Prod | Fernet key for encrypting webhook signing secrets at rest |
| `PRIVATE_KEY_ENCRYPTION_KEY` | Prod | Fernet key for encrypting user ML-DSA private keys in DB |
| `SERVER_DSA_PRIVATE_KEY` | Prod | Persistent ML-DSA-65 server signing key (base64) |
| `SERVER_DSA_PUBLIC_KEY` | Prod | Corresponding public key for audit log verification |
| `SERVER_DSA_CREATED_AT` | Prod | ISO timestamp of server key creation |
| `PQC_PROVIDER` | Prod | Must be set to a non-`reference` value in production |
| `PQC_PROVIDER_URL` | Prod | URL to the external liboqs/HSM PQC adapter |
| `CORS_ORIGINS` | Prod | Comma-separated allowed origins (no wildcards in production) |
| `ALLOWED_HOSTS` | Prod | Comma-separated allowed hostnames |
| `REDIS_URL` | Prod | For distributed rate limiting and session storage |
| `ALPACA_API_KEY` | No | Alpaca paper trading key |
| `ALPACA_SECRET_KEY` | No | Alpaca paper trading secret |

---

## 🔒 Security Architecture

### Cryptographic Specifications

| Algorithm | Role | Key sizes | Compliance |
|---|---|---|---|
| **ML-KEM-768** | Key encapsulation (post-quantum) | pk: 1184B · sk: 2400B · ct: 1088B · ss: 32B | FIPS 203 |
| **ML-DSA-65** | Digital signatures (post-quantum) | pk: 1952B · sk: 4032B · sig: 3309B | FIPS 204 |
| **X25519** | Classical hybrid KEM leg | pk: 32B · ss: 32B | RFC 7748 |
| **HKDF-SHA256** | Session key derivation | 32B output | RFC 5869 |
| **RS256 (RSA-2048)** | JWT signing | 2048-bit | RFC 7518 |
| **PBKDF2-SHA256** | Password hashing | 200,000 rounds | NIST SP 800-132 |
| **AES-256-GCM** (Fernet) | Key/secret encryption at rest | 256-bit | NIST SP 800-38D |

### Production Hardening Checklist

- [ ] Generate and persist `JWT_PRIVATE_KEY` / `JWT_PUBLIC_KEY` (RS256)
- [ ] Set and store `SERVER_DSA_PRIVATE_KEY` / `PUBLIC_KEY` (audit log integrity across restarts)
- [ ] Configure `WEBHOOK_ENCRYPTION_KEY` and `PRIVATE_KEY_ENCRYPTION_KEY` (Fernet)
- [ ] Replace `reference` PQC backend with liboqs/HSM adapter
- [ ] Enable PostgreSQL with encrypted connections
- [ ] Configure Redis with AUTH password and AOF persistence
- [ ] Set strict `CORS_ORIGINS` and `ALLOWED_HOSTS` (no wildcards)
- [ ] Place TLS 1.3 certificates at `deploy/tls/`
- [ ] Rotate ML-KEM-768 and ML-DSA-65 keys within 90-day policy

Read [SECURITY.md](SECURITY.md) for the full threat model and responsible disclosure policy.

---

## ✅ Verification

```bash
# Syntax check all Python modules
python -m py_compile backend/main.py \
    backend/schemas.py backend/models.py \
    backend/crypto/pqc.py \
    backend/services/auth_service.py \
    backend/services/signal_engine.py \
    backend/services/trading_service.py \
    backend/services/portfolio_service.py \
    backend/services/security_service.py \
    backend/services/backtest_service.py \
    backend/services/integration_service.py

# JavaScript syntax check
node --check frontend/app.js

# Run test suite
pytest -q

# Docker health check (uses /health/ready — validates DB + Redis)
docker compose up --build -d
curl http://localhost:8000/health/ready
```

---

## 🤝 Contributing

Contributions are welcome! Good first issues are tagged on [GitHub Issues](https://github.com/BugHunterX2101/quantumsentinel-web/issues?q=label%3A%22good+first+issue%22).

```bash
git checkout -b feature/your-feature
# make changes
git commit -m "feat: describe your change"
git push origin feature/your-feature
# Open a Pull Request
```

Please read [SECURITY.md](SECURITY.md) before reporting vulnerabilities.

---

## 📄 License

Apache-2.0. See [LICENSE](LICENSE).

---

<div align="center">

Built with ⚛️ post-quantum cryptography · 📈 real market data · 🔏 signed audit trail

*QuantumSentinel is informational software. It is not financial advice and carries no certification for use in regulated financial systems.*

</div>
